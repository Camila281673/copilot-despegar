# Dependencias del proyecto "Recover the Long Tail"
# Versiones fijadas para que el entorno sea reproducible.
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.5
matplotlib==3.9.2
streamlit==1.38.0
# Opcional: solo se usa si querés generar los mensajes con Claude (API key).
anthropic==0.34.2
